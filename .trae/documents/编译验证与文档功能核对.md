## 现状结论（基于代码与文档静态分析）
- 当前项目没有独立的“项目文档”模块（无 documents 数据表、无文档 CRUD/列表/详情 UI、无 Markdown/PDF 渲染或导出逻辑）。
- 代码里与“文档”最接近的内容主要是：
  - 时间轴智能搜索的示例文案里提到 PDF（仅 tooltip 文案，不是功能）：[Timeline.tsx:L387-L437](file:///d:/Demo/memflow/src/components/Timeline.tsx#L387-L437)
  - 知识图谱把 OCR 文本抽取出来的关键词归为 `doc` 组节点（只是关键词节点，不是文件/文档实体），且关键词提取是占位实现：
    - [graph.rs:L36-L148](file:///d:/Demo/memflow/src-tauri/src/graph.rs#L36-L148)
    - [KnowledgeGraph.tsx:L175-L221](file:///d:/Demo/memflow/src/components/KnowledgeGraph.tsx#L175-L221)
  - AI 提示词里“尝试从窗口标题或 OCR 提取 .docx/.pdf 路径”，用于 related_files 字段，不等于文档功能： [ai/mod.rs:L173-L204](file:///d:/Demo/memflow/src-tauri/src/ai/mod.rs#L173-L204)
- 设计文档提到“导出为 Markdown/纯文本”属于规划项，代码未落地： [设计文档:L204-L214](file:///d:/Demo/memflow/MEMFLOW_%E4%BC%98%E5%8C%96%E4%B8%8E%E6%96%B0%E5%A2%9E%E5%8A%9F%E8%83%BD_%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3.md#L204-L214)

## 编译与运行验证（确认工程可构建、并用运行态核对“文档功能”）
1. 安装依赖并做前端静态检查：pnpm install、pnpm type-check、pnpm lint。
2. 做后端静态检查与单测：pnpm test:setup（Windows）或 cargo check/test。
3. 进行 Tauri 构建：pnpm tauri:build（验证打包前的完整构建链路）。
4. 运行开发态应用并逐项核对：
   - 顶部视图入口是否存在“文档/导出/Markdown/PDF”相关入口（当前静态分析为无，运行态确认）。
   - 时间轴/画廊/问答/统计/图谱里是否有导出、文档预览或文档列表等交互。

## 文档功能核对清单（用于判定“是否已完全实现”）
- 数据层：是否有 documents 表或等价模型；是否能关联 activity_logs；是否有迁移脚本。
- 后端能力：是否有导出/生成文档的 commands（导出 Markdown/PDF/纯文本/报告等）。
- 前端能力：是否有文档列表/详情页；Markdown 渲染/编辑；导出/复制按钮；错误处理与进度提示。
- 端到端：从活动数据生成“日报/周报/复盘”并导出为 Markdown/文本的路径是否可用。

## 交付物（在你确认后我将实际执行并给出）
- 一份“可编译/不可编译”的构建报告（包含失败原因与修复建议）。
- 一份“文档功能实现度”报告：已实现点/缺失点/对应代码位置。
- 如发现明显缺失（例如脚本/依赖不一致），给出最小修复补丁建议（不直接引入新功能，先保证可构建与一致性）。